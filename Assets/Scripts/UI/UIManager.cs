using UnityEngine;


///Clase encargada del HUDController -> escucha los eventos del player stats y actualiza los corazones, barra de estamina
/// monedas etc
/// Pause menu, GameOverScreen, etc -> solo llaman a metodos del game manager.
public class UIManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
