using UnityEngine;

/// Clase encargada de la vida, estamina, objetos recogidos en la run actual. Se resetea al empezar el nivel de nuevo,
/// guarda los records.
public class PlayerStars : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
