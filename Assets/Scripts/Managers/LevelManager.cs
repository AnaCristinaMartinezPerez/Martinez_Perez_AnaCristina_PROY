using UnityEngine;


///Clase encargada de spawmear al juagor, contar monedas totales del nivel, detectar cuando se completa, y llamar el metodo de savelevel,
/// y resetfornewlevel
public class LevelManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
